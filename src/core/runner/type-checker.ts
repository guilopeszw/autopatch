/**
 * Scaffold for the in-memory compiler gate using Project.getPreEmitDiagnostics().
 * Implement alongside its first behavior test. Compiler errors must block saving
 * or committing patches; never spawn a tsc subprocess for validation.
 */
export {};
