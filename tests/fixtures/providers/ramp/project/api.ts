/** Deliberately incomplete local adapter; this is not a provider SDK. */
export interface ApiTransactionCardHolder {
  department_id?: string;
  department_name?: string;
  first_name?: string;
  last_name?: string;
  location_id?: string;
  location_name?: string;
  user_id?: string;
}
