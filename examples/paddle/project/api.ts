/** Incomplete local projection of Paddle's subscription portal URLs. */
export interface CustomerPortalSessionUrlsSubscriptionsItem {
  id: string;
  cancel_subscription: string;
    "update_subscription_payment_method": string;
}
