{
  "name": "autopatch",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic, AST-driven API migrations with an in-memory TypeScript verification gate.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "autopatch": "tsx bin/autopatch.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "15.0.0",
    "ts-morph": "28.0.0"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "tsx": "4.23.13",
    "vitest": "5.0.0"
  }
}
